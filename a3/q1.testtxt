No late days and no time to do this assignment :(

I didn't really get to test it at all. Some output:

empty semaphore count is -1 for some reason. This is the empty
semaphore inside the q1kvqueue.cc file. 

I was trying to debug this when the time ran out.

I think most of my logic is there in the header and cc files however,
but at the moment, nothing works
